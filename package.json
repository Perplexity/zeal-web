{
  "name": "root",
  "private": true,
  "scripts": {
    "build-local": "lerna bootstrap && cd packages/website && npm run build && cd ../api && npm run build",
    "build": "npm i typescript -g && lerna bootstrap && lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
