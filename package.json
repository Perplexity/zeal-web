{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm i ts-node typescript -g && cd packages/api && npm install && npm run build && cd ../api && npm install && npm run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
