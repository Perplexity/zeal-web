{
  "name": "root",
  "private": true,
  "scripts": {
    "build-local": "lerna bootstrap && cd packages/website && npm run build && cd ../api && npm run build",
    "build": "lerna bootstrap && lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.3.5"
  }
}
