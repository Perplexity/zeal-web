{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd packages/website && npm install && npm run build && cd ../api && npm install && npm run build"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.29.0",
    "@typescript-eslint/parser": "^4.29.0",
    "eslint": "^7.32.0",
    "eslint-plugin-react": "^7.24.0",
    "lerna": "^4.0.0"
  }
}
