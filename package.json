{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=DEPENDENCY_ENV lerna bootstrap && NODE_ENV=BUILD_ENV lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
